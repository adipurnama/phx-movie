defmodule MovieWeb.ActorControllerTest do
  # use MovieWeb.ConnCase

  # describe "index" do
  #   test "lists all actors", %{conn: conn} do
  #     conn = get(conn, Routes.actor_path(conn, :index))
  #     assert html_response(conn, 200) =~ "Listing Actors"
  #   end
  # end
end
